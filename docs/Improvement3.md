Verdict

Much better, but still not complete.

Implemented:

Shared run_post_agent_evaluation(agent_trace)
Called from both AgentDefinitionViewSet.run() and AgentChainExecutor
Creates EvaluationRun
Creates AgentEvaluationHistory
Creates AgentImprovementRecommendation
Accepting recommendation now creates a PromptTemplate + AgentPromptAssignment
Recommendation queryset is now ownership-filtered
Default quality evaluation no longer has the bad 100-source rule

Remaining issues:

Workflow analytics UI still has hardcoded KPI values
Acceptance Rate: 88.6%
Avg Revisions: 1.5
Hallucination Risk: 1.2%
WorkflowAnalytics tests are stale
They still expect mock agents:
Web Researcher
Summarizer
Report Writer
Reject improvement is frontend-only
The UI hides rejected recommendations locally but does not call a backend reject endpoint.

Evaluation engine ignores each template’s real output schema
It forces all evaluators into:

score
feedback

That works for basic scoring, but it does not preserve rich outputs like unsupported claims, weak sources, or required fixes.

Improvement recommendations are generated by another LLM call
Acceptable for MVP, but should be schema-grounded from evaluator results, not just freeform feedback.
Fix next
1. Replace hardcoded WorkflowAnalytics KPI cards with API values.
2. Update WorkflowAnalytics tests to mock the API response instead of expecting old mock agents.
3. Add backend reject endpoint for recommendations.
4. Use EvaluationTemplate.output_schema during evaluator execution.
5. Store rich evaluator outputs, not just score/feedback.

The improvement loop is now functionally present, but the analytics UI and evaluator richness still need cleanup.